logging:
  level:
    root: debug
    io.kuark.xxx: debug

cache:
  config:
    enabled: true
    strategy: LOCAL_REMOTE

spring:
  cache:
    caffeine:
      spec: maximumSize=500, expireAfterAccess=3600s