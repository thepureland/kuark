server:
  port: 9201


spring:
  application:
    name: gateway-server
  cloud:
    gateway:
      enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8001/eureka/


logging:
  level:
    org.springframework.cloud.gateway: debug